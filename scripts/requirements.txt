# Dependencies for News Pipeline Worker (GitHub Actions)
# Install with: pip install -r scripts/requirements.txt

# Firebase Admin SDK (Firestore)
firebase-admin>=6.0.0

# Cloudinary (image upload)
cloudinary>=1.40.0

# HTTP Requests
requests>=2.31.0

# Cerebras Cloud SDK (LLM — GPT-OSS 120B, llama3.1-8b)
cerebras-cloud-sdk>=1.0.0

# g4f (gpt4free) — multi-provider image generation (Flux, DALL-E, SDXL)
g4f[image]>=7.5.7

# Image content verification (pixel/colour metrics) — HARD dependency.
# Declared explicitly so verification is never silently skipped if the g4f
# image extra changes what it pulls in.
pillow>=10.0.0
numpy>=1.24.0

# Environment Variables (local dev only)
python-dotenv>=1.0.0

# RapidFuzz (fast C++ fuzzy string matching for title deduplication)
rapidfuzz>=3.0.0
